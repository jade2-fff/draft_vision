# 飞镖自瞄系统设计

## 管线架构

```
相机采图 → 飞镖靶检测(HSV+圆) → PnP解算(3D位姿) → EKF跟踪 → 输出(归一化偏移) → 串口 → 下位机
```

## 各模块职责

| 模块 | 功能 | 算法 | 输出 |
|------|------|------|------|
| detector | HSV绿色靶面检测 | 轮廓提取 + 圆度/填充率/面积/宽高比过滤，预留YOLO接口 | 靶心像素坐标、半径、bbox |
| solver | PnP位姿解算 | IPPE：像素→相机系→世界系坐标变换链 | 靶心3D坐标(m)、距离(m) |
| tracker | 图像空间EKF跟踪 | 8状态([cx,vx,cy,vy,w,vw,h,vh]) + 4状态机(LOST→DETECTING→TRACKING→TEMP_LOST) | 稳定跟踪目标 |
| aimer | 弹道补偿 | 抛物线弹道+空气阻力（**当前未启用**） | yaw/pitch调整量 |

## 上位机 vs 下位机分工

```
上位机                           下位机
┌──────────────┐   串口        ┌──────────────────┐
│ 检测靶心      │ ──────────→  │ 接收距离+yaw      │
│ PnP解算距离   │              │ 查表/算弹道pitch   │
│ EKF稳定跟踪   │              │ 驱动yaw电机        │
│ 输出偏移      │              │ 控制发射机构       │
└──────────────┘              └──────────────────┘
```

- **上位机**：检测靶心，PnP解算距离和方向，发*(距离, yaw)* 或归一化偏移
- **下位机**：接收数据，根据距离查表/算弹道pitch，驱动yaw电机和发射机构
- **上位机不碰pitch**——飞镖pitch是固定机械角，下位机根据距离决定发射时机和力度

## 跟踪器状态机

```
LOST → 检测到目标 → DETECTING → 连续5帧 → TRACKING
  ↑                      │                      │
  └────超时──────── TEMP_LOST ←──暂时丢失────────┘
```

- **LOST**: 无目标，不输出
- **DETECTING**: 检测到但未稳定，积累帧数
- **TRACKING**: 稳定跟踪，EKF预测+更新，正常输出
- **TEMP_LOST**: 短暂丢失（容忍15帧），EKF纯预测不更新

## 坐标变换链

```
像素(u,v) → PnP(IPPE) → 相机系(Xc,Yc,Zc) → 手眼标定 → 云台系(Xg,Yg,Zg) → IMU姿态 → 世界系(Xw,Yw,Zw)
```

距离 = |position_world|，yaw = atan2(Yw, Xw)

## 为什么不做弹道补偿

1. 飞镖只有 yaw 轴能动，pitch 是固定机械角
2. 下位机已有弹道标定曲线（距离→pitch查表）
3. 上位机只提供视觉指向，弹道全权由下位机控制

## 待定：串口协议

当前用归一化偏移（float, [-1,1]），兼容区域赛 draft 协议。  
需与下位机方确认：
- 数据字段：距离(float) + yaw角(float)，还是归一化偏移
- 波特率/帧头/校验等

## 弹道标定方案（后续）

打靶心法——多距离各5×5发命中后记录(距离, 命中pitch)，Python脚本拟合下位机pitch补偿曲线。  
上位机不跑弹道公式，pitch全权由下位机控制。
