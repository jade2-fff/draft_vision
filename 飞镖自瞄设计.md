# 飞镖自瞄系统设计

## 管线架构

```
相机采图 → 飞镖靶检测(HSV+圆) → PnP解算(3D位姿) → EKF跟踪 → 输出(距离+yaw) → 串口 → 下位机
```

## 各模块职责

| 模块 | 功能 | 输出 |
|------|------|------|
| detector | HSV绿色靶面检测，轮廓+圆度过滤 | 靶心像素坐标、半径 |
| solver | PnP解算，像素→相机→世界坐标系链 | 靶心3D坐标(m)、距离 |
| tracker | 图像空间8状态EKF，状态机(LOST→DETECTING→TRACKING) | 稳定跟踪目标 |
| 输出层 | 距离+yaw角 → 串口发送 | 下位机可用的数值 |

## 上位机 vs 下位机分工

- **上位机**：检测靶心，PnP解算距离和方向，发*(距离, yaw)*
- **下位机**：接收数据，根据距离查表/算弹道pitch，驱动yaw电机和发射机构
- **上位机不碰pitch**——飞镖pitch是固定机械角，下位机根据距离决定发射时机和力度

## 待定：串口协议

当前用 `VisionToGimbal`(28字节云台协议)，飞镖需要精简版。需与下位机方确认：
- 数据字段：距离(float) + yaw角(float) + 开火标志(bool)
- 波特率/帧头/校验等

## 弹道标定方案（后续）

打靶心法——多距离各N发命中后记录(距离, 命中pitch)，拟合下位机pitch补偿曲线。

当前不跑上位机弹道公式，pitch全权由下位机控制。
